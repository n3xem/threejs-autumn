{
  "type": "module",
  "dependencies": {
    "three": "^0.158.0"
  },
  "devDependencies": {
    "cpx": "^1.5.0",
    "npm-run-all": "^4.1.5",
    "rimraf": "^5.0.5",
    "typescript": "^5.4.2",
    "vite": "^4.5.0"
  },
  "scripts": {
    "dev": "vite",
    "clean": "rimraf build",
    "build": "run-s -l clean build:**",
    "build:tsc": "tsc",
    "build:static": "cpx static/** build"
  }
}
